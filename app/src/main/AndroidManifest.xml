<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.question">

    <application
        android:name=".util.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/sc_happy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sc_happy"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <!-- android:theme="@style/AppTheme"> -->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityGameIntroPart1" />
        <activity android:name=".ActivityGameIntroPart2" />
        <activity android:name=".ActivityGameQuestions" />
        <activity android:name=".AnimalListActivity"/>
    </application>

</manifest>