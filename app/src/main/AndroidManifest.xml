<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.question">

    <application
        android:name=".util.MainApplication"
        android:allowBackup="false"
        android:icon="@mipmap/sc_happy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sc_happy"
        android:supportsRtl="true"
        android:theme="@style/ConsoleTheme">

        <!-- android:theme="@style/AppTheme"> -->

        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityGameIntroPart1" android:theme="@style/ConsoleTheme"/>
        <activity android:name=".ActivityGameIntroPart2" android:theme="@style/ConsoleTheme"/>
        <activity android:name=".PlayerMatchActivity" android:theme="@style/ConsoleTheme"/>
        <activity android:name=".AnimalListActivity" android:theme="@style/ConsoleTheme"/>
        <activity android:name=".MainMenuActivity" android:theme="@style/ConsoleTheme"/>
        <activity android:name=".MachineMatchActivity" android:theme="@style/ConsoleTheme"/>
    </application>

</manifest>